import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  field: {
    margin: 0,
    borderWidth: 1,
    borderColor: '#303030',
    borderRadius: 8,
    height: 40,
    paddingHorizontal: 4,
  },
});
